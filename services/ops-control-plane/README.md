Runnable Go scaffold for the ops-control-plane migration demo.
Mirrors services/pricing-engine with a minimal build, test, and HTTP server path.
Run `go run ./cmd/server` and check `/healthz` or `/v1/controls`.
